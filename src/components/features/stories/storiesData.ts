export interface StoryItem {
  id: string;
  slug: string;
  name: string;
  location: string;
  title: string;
  excerpt: string;
  image: string;
  featured: boolean;
}

export interface ImpactItem {
  icon: string;
  title: string;
  description: string;
}
